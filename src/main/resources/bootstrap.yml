#Server Configuration
server:
  port: 8765

#Spring Configuration
spring:
  application: 
    name: gateway-service
  cloud: 
    config: 
      uri: https://gambit-config-server-brash-porcupine.cfapps.io

#Eureka Configuration
eureka:
  client:
    register-with-eureka: true
